mixin video(info)
    div 
        h4=info.titled
        ul
            li #{info.rating}/5. 
            li #{info.comment} comments.
            li Posted #{info.createAt}
            li #{info.views} views.